name: Prefrontal CI (Stable)

on:
  push:
    branches: [main]

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      # =========================
      # OLLAMA INSTALL
      # =========================

      - name: Install Ollama (Linux/Windows)
        if: runner.os != 'macOS'
        run: |
          curl -fsSL https://ollama.com/install.sh | sh

      - name: Install Ollama (Windows fix)
        if: runner.os == 'Windows'
        shell: powershell
        run: irm https://ollama.com/install.ps1 | iex

      - name: Add Ollama PATH (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: echo "$env:LOCALAPPDATA\Programs\Ollama" >> $env:GITHUB_PATH

      # =========================
      # RUN PIPELINE
      # =========================

      - name: Run CI
        run: node tests/run-ci.js
